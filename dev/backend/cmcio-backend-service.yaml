apiVersion: v1
kind: Service
metadata:
  name: cmcio-backend-service
  namespace: dev  
spec:
  selector:
    app: cmcio-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort